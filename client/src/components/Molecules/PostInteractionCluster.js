import { } from '@material-ui/core';  
import ActionIconButton from '../Atoms/ActionIconButton';

/**
 * Summary:
 * Group of button that appear on the bottom of all post. Shows how many and allows you to like, comments, and share.
 * 
 * Props: 
 * 
 */
export default props => {
	return (
		<div>
      		<p>Post Interaction Cluster</p>
    	</div>
  	);
}