import { } from '@material-ui/core';  

/**
 * Summary:
 * Takes in an image and displays it as a circle. All instances and uses of circle images should be the same size. On hover should show pop up with information about the user.
 * 
 * Props: 
 * image
 * action: option prop that if provided will be ran onClick on image. could be a link to a profile or open messenger.
 * hoverInformation: Object of information to display.
 */
export default props => {
  	return (
    	<>
     		<p>Circle Image</p>
    	</>
  	);
}